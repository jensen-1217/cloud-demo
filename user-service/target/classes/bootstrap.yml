spring:
  application:
    name: userservice
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: localhost:8999
        file-extension: yaml
        refresh-enabled: true